# Mad Mikes Car System
#### by Jordan Barrilleaux

### Run
in directory with docker-compose.yaml execute ```docker-compose up --build```
this sould build and start the backend container, frontend container and mongodb database